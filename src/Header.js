import React from "react";
import ReactDOM from "react-dom/client";

export default function Header() {
  return (
    <nav className="navbar navbar-dark">
      <div className="container-fluid">To Do</div>
    </nav>
  );
}